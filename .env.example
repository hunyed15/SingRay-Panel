PANEL_HOST=127.0.0.1
PANEL_PORT=3000
PANEL_DB=./data/panel.db
PANEL_JWT_SECRET=change-me-openssl-rand-hex-32
# 字段加密密钥(节点凭据/SSH 私钥落库加密)。从旧面板迁移时必须设为旧面板的 APP_SECRET,否则旧密文解不开!
# 全新安装:openssl rand -hex 32 生成,与 PANEL_JWT_SECRET 用不同的值即可
PANEL_APP_SECRET=change-me-openssl-rand-hex-32
PANEL_ADMIN_USER=admin
PANEL_ADMIN_PASSWORD=admin888
